import { newVM, VM } from './vm';

export const Forth = (): unknown => {
    const forth: VM = newVM();
    // Definitions(forth);
    // Input(forth);
    // NumericOperations(forth);
    // BooleanOperations(forth);
    // StackOperations(forth);
    // MemoryOperations(forth);
    // ControlStructures(forth);
    // Output(forth);
    // JsInterop(forth);
    // Include(forth);
    // Interpreter(forth);

    return forth;
};
